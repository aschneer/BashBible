# Useful Libraries

## C++ stdlib

```cpp
#include <functional>
    std::function
#include <memory>
    std::shared_ptr
    std::make_shared()
    std::unique_ptr
    std::make_unique()
#include <utility>
    std::forward
#include "absl/strings/str_cat.h"
    absl::StrCat()
#include "absl/strings/str_format.h"
```
